import { flyAngelDeviant } from "@/assets";

const NewDeviant = () => {
  return (
    <div
      className="min-h-screen"
      style={{ background: `top no-repeat url(${flyAngelDeviant})`, backgroundSize: "contain", backgroundPositionY: "60px" }}
    ></div>
  );
}

export default NewDeviant